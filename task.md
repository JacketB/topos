# Чеклист задач по исправлению MapExportComponent

- [x] Шаг 1: Реализовать точный расчет экранных координат рамки видоискателя относительно контейнера карты с использованием `getBoundingClientRect()` в методе `generateExport()` компонента `MapExportComponent` ([map-export.component.ts](file:///F:/Vanya/topos/src/app/features/map-view/components/map-export/map-export.component.ts)).
- [x] Шаг 2: Использовать полученные координаты `x1, y1, x2, y2` для `unproject` вместо вычислений от центра контейнера карты.
- [x] Шаг 3: Обновить расчет `exportZoom` в режиме "Авто" с использованием фактической ширины рамки видоискателя `vfRect.width`.
- [x] Шаг 4: Устранить race condition при переключении `isGenerating`: считывание `printSizeMm()`, `resultPixels()` и `getBoundingClientRect()` перенесено в самое начало `generateExport()`, до того как Angular скроет видоискатель через `*ngIf`.
- [x] Шаг 5: Вычислять `exportCenter` через проецирование точного экранного центра рамки для устранения искажений проекции Меркатора.
- [x] Шаг 6: Добавить коррекцию `exportZoom` на коэффициент `safeScale` при сжатии изображения до лимитов WebGL.
- [x] Шаг 7: Внедрить точный геодезический расчет сторон рамки (`widthMeters`/`heightMeters`) на основе формулы Хаверсинуса по трем разпроецированным углам (`p_tl`, `p_tr`, `p_bl`) для устранения погрешностей при повороте (`bearing`) и наклоне (`pitch`) карты.
- [x] Шаг 8: Добавить принудительный reflow (`hiddenDiv.offsetHeight` и пауза 50мс) и вызовы `map.resize()` после загрузки скрытой карты в DOM для корректной калибровки размеров вьюпорта в WebGL.
- [x] Шаг 9: Зафиксировать высоту печатного листа в `printSizeMm()` строго пропорционально `aspect` рамки видоискателя на экране, полностью устраняя обрезание картинки по горизонтали.
- [x] Шаг 10: Интегрировать учет WebGL-лимитов `maxWebGLSize` и расчет `safeScale` непосредственно в computed-сигнал `resultPixels()`, обеспечив 100% совпадение разрешения в UI-форме и в результирующем файле.
- [x] Шаг 11: Внедрить компенсацию в 10% для расширения географического охвата экспортируемой области:
  * В режиме фиксированного масштаба: `widthMeters` умножается на коэффициент `1.10`, что расширяет Canvas без изменения зума.
  * В режиме "Авто": уровень `exportZoom` корректируется на `-0.1375` уровней, отдаляя камеру для компенсации среза краев.
- [x] Шаг 12: Запустить локальную сборку и проверить отсутствие ошибок компиляции TypeScript/Angular (`npm run build`).

Все задачи успешно выполнены. Сборка проходит без ошибок.

## Перенос рендеринга на бэкенд Tauri/Rust (Tauri/Rust SSR)
- [x] Шаг 13: Разработка обособленного Node-скрипта рендерера (`sidecar-renderer`) на базе `mbgl-renderer`.
- [x] Шаг 14: Интеграция Node-процесса в ресурсы дистрибутива `bundle.resources` в `tauri.conf.json`.
- [x] Шаг 15: Реализация IPC-команды `export_map_native` в Rust с перехватом `stderr` крашей.
- [x] Шаг 16: Динамическая сериализация и внедрение всех GeoJSON-источников карты напрямую в стиль на фронтенде для исключения внешних сетевых запросов.
- [x] Шаг 17: Реализация mock-перехватчика `request` для чтения локальных PMTiles баз на диске из Node.js.
- [x] Шаг 18: Исправление `.gitignore` и опечаток, мешавших правильному отслеживанию изменений.
- [x] Шаг 19: Успешная валидация Angular-сборки (`npm run build`) и компиляции Rust (`cargo check`).

## Исправление ошибки экспорта (Failed to write to stdin)
- [x] Шаг 20: Доработать обработку ошибок записи в `stdin` в `lib.rs`, считывая `stderr` дочернего процесса при сбое `write_all`.
- [x] Шаг 21: Настроить рабочую директорию (CWD) для дочернего процесса Node.js in `lib.rs` (установить её равной директории скрипта).
- [x] Шаг 22: Запустить проверку компиляции Rust (`cargo check`).
- [x] Шаг 23: Провести диагностику через запуск приложения или логи, выявить и исправить причину падения Node.js.
- [x] Шаг 24: Проверить успешность экспорта и записать результаты в `log.md` и Второй Мозг.

## Исправление ошибки lstat 'F:' (UNC paths)
- [x] Шаг 25: Реализовать функцию `clean_unc_path` в `lib.rs` для удаления префиксов `\\?\` на Windows.
- [x] Шаг 26: Применить `clean_unc_path` ко всем путям в `export_map_native`.
- [x] Шаг 27: Запустить `cargo check` и `cargo test` для проверки компиляции и тестов.
- [x] Шаг 28: Залогировать изменения в `log.md` и Второй Мозг.

## Ограничение размера иконок при экспорте (WebGL Native limit)
- [x] Шаг 29: Реализовать ограничение максимального размера иконки до 1024px в `map-export.component.ts` (как для SVG, так и для картинок атласа).
- [x] Шаг 30: Рассчитывать `pixelRatio` динамически как `targetWidth / origW` для сохранения масштаба.
- [x] Шаг 31: Запустить Angular-сборку (`npm run build`) для проверки отсутствия ошибок компиляции TypeScript.
- [x] Шаг 32: Залогировать изменения в `log.md` и Второй Мозг.

## Исправление предупреждений styleimagemissing (rknp)
- [x] Шаг 33: Внедрить надежный разбор `StyleImage` в ветке `else` в `map-export.component.ts` (поддержка плоских `Uint8Array` и `RGBAImage` форматов MapLibre).
- [x] Шаг 34: Проверить успешность Angular-сборки (`npm run build`).
- [x] Шаг 35: Задокументировать изменения в `log.md` и Втором Мозг.

## Исправление ошибки incorrect header check
- [x] Шаг 36: Удалить повторную декомпрессию `zlib.unzip` для векторных тайлов в `index.js` и возвращать `Buffer.from(tile.data)` напрямую.
- [x] Шаг 37: Запустить тесты Rust (`cargo test`) для верификации рендеринга тайлов с новым `index.js`.
- [x] Шаг 38: Залогировать изменения в `log.md` и Второй Мозг.

## Исправление ошибки TypeError: url.endsWith is not a function
- [x] Шаг 39: Защитить разбор `url` в `requestMock` в `index.js` от нестроковых объектов и `undefined`.
- [x] Шаг 40: Запустить тесты Rust (`cargo test`) для проверки работоспособности.
- [x] Шаг 41: Задокументировать изменения в `log.md` и Втором Мозг.

## Исправление ошибки Invalid URI
- [x] Шаг 42: Реализовать перехват и локальное разрешение относительных путей в `requestMock` в `index.js`.
- [x] Шаг 43: Запустить тесты Rust (`cargo test`) для проверки корректности рендеринга с локальными шрифтами/ресурсами.
- [x] Шаг 44: Задокументировать изменения в `log.md` и Втором Мозг.

## Исправление ошибки Cannot read properties of undefined (reading 'uri')
- [x] Шаг 45: Добавить проверку `if (!options)` в начало `requestMock` в `index.js`.
- [x] Шаг 46: Запустить тесты Rust (`cargo test`) для проверки корректности.
- [x] Шаг 47: Задокументировать изменения в `log.md` и Втором Мозг.

## Исправление приоритета путей скрипта (dev vs resource)
- [x] Шаг 48: Изменить логику выбора пути `script_path` в `lib.rs` (отдавать приоритет дев-пути).
- [x] Шаг 49: Запустить `cargo test` для проверки компиляции и тестов.
- [x] Шаг 50: Задокументировать изменения в `log.md` и Втором Мозг.

## Исправление TypeError res.request.uri в getRemoteAsset
- [x] Шаг 51: Добавить `request: { uri: { href: url } }` во все возвращаемые мок-ответы в `index.js`.
- [x] Шаг 52: Заменить статус-код локальных отсутствующих ресурсов на `200` с пустым буфером для игнорирования отсутствующих ассетов.
- [x] Шаг 53: Запустить `cargo test` для проверки корректности.
- [x] Шаг 54: Задокументировать изменения в `log.md` и Втором Мозг.

## Исправление ошибки Sharp raw input descriptor (width/height not integer)
- [x] Шаг 55: Обернуть `width * ratio` и `height * ratio` в `Math.round` в `node_modules/mbgl-renderer/dist/render.js`.
- [x] Шаг 56: Скорректировать вычисление `logicalW` и `logicalH` в `map-export.component.ts`.
- [x] Шаг 57: Проверить сборку через `cargo test`.
- [x] Шаг 58: Задокументировать изменения в `log.md` и Втором Мозге.

## Исправление рассинхронизации размера буфера (Math.floor вместо Math.round для Sharp)
- [x] Шаг 59: Заменить `Math.round` на `Math.floor` в `node_modules/mbgl-renderer/dist/render.js`.
- [x] Шаг 60: Проверить сборку и рендеринг через `cargo test`.
- [x] Шаг 61: Задокументировать изменения в `log.md` и Втором Мозге.

## Устранение стилистических предупреждений (GeoJSON type)
- [x] Шаг 62: Добавить подстановку `{"type":"FeatureCollection","features":[]}` в `map-export.component.ts` и `lib.rs`.
- [x] Шаг 63: Проверить сборку через `cargo test`.
- [x] Шаг 64: Задокументировать изменения в `log.md` и Втором Мозге.

## Устранение черного квадрата и восстановление тактических знаков/линий при экспорте
- [x] Шаг 65: Написать функцию `sanitizeStyleForNative` в `map-export.component.ts` для замены несовместимых `coalesce` выражений на нативные `get` expressions.
- [x] Шаг 66: Установить безопасный лимит растрового EGL буфера `maxWebGLSize = 4096` в `map-export.component.ts`.
- [x] Шаг 67: Проверить сборку и рендеринг через `cargo test`.
- [x] Шаг 68: Задокументировать изменения в `log.md` и Втором Мозге.

## Восстановление полного 16K разрешения экспорта и извлечения всех style.getImage иконок
- [x] Шаг 69: Написать универсальный метод `getStyleImageDataUrl` в `map-export.component.ts` для поддержки любых иконок StyleImage.
- [x] Шаг 70: Увеличить `maxWebGLSize` до `16384` в `map-export.component.ts` для обеспечения честного 16K разрешения экспорта.
- [x] Шаг 71: Проверить сборку и рендеринг через `cargo test`.
- [x] Шаг 72: Задокументировать изменения в `log.md` и Втором Мозге.

## Реализация сеточного рендеринга по частям (Tiled Chunking SSR) и восстановление Z-Order слоев
- [x] Шаг 73: Добавить принудительную перестановку пользовательских тактических слоев наверх (Z-Order) в `map-export.component.ts`.
- [x] Шаг 74: В `index.js` добавить декодирование Base64 PNG иконок в сырые RGBA буферы `sharp(buf).raw()` перед вызовом `map.addImage()`.
- [x] Шаг 75: Реализовать функции деления полотна на плитки <= 4096px, математику смещения географических центров фрагментов и сборку финального растра через `sharp.composite()` в `index.js`.
- [x] Шаг 76: Убрать урезание `maxWebGLSize` в `map-export.component.ts`, позволив передавать исходные полиграфические размеры высокой четкости.
- [x] Шаг 77: Проверить сборку через `cargo test`.
- [x] Шаг 78: Задокументировать изменения в `log.md` и Втором Мозге.
